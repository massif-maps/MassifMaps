<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- star sky mode only: the camera preview the transparent map is composited over -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:hardwareAccelerated="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- The example gallery: what the app opens on. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/GalleryTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- One example. Exported so a capture run can drive it, naming the example with an
             "example" string extra. -->
        <activity
            android:name=".ExampleActivity"
            android:exported="true"
            android:theme="@style/ExampleTheme" />

        <activity
            android:name=".CodeActivity"
            android:exported="false"
            android:theme="@style/GalleryTheme" />

        <!-- The BENCH: the composable debugging/measurement map the root CLAUDE.md documents.
             singleTop so 'am start' with extras reconfigures the running map instead of
             relaunching it - a relaunch rebuilds every cache, which hides stale-redraw bugs. -->
        <activity
            android:name=".BenchActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme" />

    </application>

</manifest>
